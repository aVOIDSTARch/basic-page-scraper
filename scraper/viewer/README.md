# Viewer — Scrape Output Browser

Run the local viewer (requires Bun):

```bash
bun ./scraper/viewer/server.ts
```

Open [http://localhost:3000/](http://localhost:3000/) to see a list of scrape output folders. Click a folder to browse its files (served from `./output/<folder>`).
